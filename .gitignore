.env
.venv
*.env
docker-compose.env # 
docker-compose.override.yml
*.pem
*.key
*.p12
*.pfx
secrets/
cosign.key
cosign.pub


# Added to .gitignore
@"

# Scan reports — never commit, contain secret-shaped strings
reports/
evidence/
trivy-report.json
gitleaks-report.json
*.report.json
"@ | Add-Content .gitignore
cosign.key
*.key
