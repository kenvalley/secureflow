# infra/kubernetes/base/configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: secureflow-config
  namespace: secureflow
data:
  # Non-sensitive service URLs only
  # All secrets migrated to Vault (Phase 1)
  AUTH_SERVICE_URL: "http://auth-service:5001"
  TRANSACTION_SERVICE_URL: "http://transaction-service:5002"