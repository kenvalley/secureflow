# New version of kustomization.yaml with vault-agent-templates.yaml
# infra/kubernetes/base/kustomization.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: secureflow

resources:
  - namespace.yaml
  - configmap.yaml
  - databases.yaml
  - auth-service.yaml
  - transaction-service.yaml
  - frontend.yaml
  - network-policies.yaml
  - vault-agent-templates.yaml




#### Old version of kustomization.yaml without vault-agent-templates.yaml

# apiVersion: kustomize.config.k8s.io/v1beta1
# kind: Kustomization

# namespace: secureflow

# resources:
#   - namespace.yaml
#   - configmap.yaml
#   - databases.yaml
#   - auth-service.yaml
#   - transaction-service.yaml
#   - frontend.yaml
