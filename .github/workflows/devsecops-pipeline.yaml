
name: Secureflow Pipeline

on:
  workflow_dispatch:
  push:
  pull_request:

jobs:
  hello-world:
    name: Hello World
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello World
        run: echo "Hello World!"